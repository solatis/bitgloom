<header>
  <h2>Anonymize
  <p>
    $if null (accountId)
      <i>Default
    $else
      #{accountId}

<div class="box">
  <form method=post action=@{AnonymizeAccountR accountId} enctype=#{formEnctype} id="anonymize-account-form">
    ^{formWidget}

<!-- <div class="box"> -->
<!--   <form method=post action=@{AnonymizeAccountR accountId}> -->
